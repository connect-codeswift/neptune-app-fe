import { MyProfilePageClient } from "@/components/profile/MyProfilePageClient";

/** Signed-in user profile — opened from the sidebar profile badge. */
export default function MyProfilePage() {
  return <MyProfilePageClient />;
}
