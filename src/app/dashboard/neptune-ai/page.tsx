import { NeptuneAiPageClient } from "@/components/neptune-ai/NeptuneAiPageClient";

/** Neptune AI — the full assistant workspace, opened from the sidebar. */
export default function NeptuneAiPage() {
  return <NeptuneAiPageClient />;
}
