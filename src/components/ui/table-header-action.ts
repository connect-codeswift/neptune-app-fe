/**
 * Shared sizing for action buttons in module table / register card headers.
 * Colors come from the Button variant (primary / tertiary) — keep those as-is.
 */
export const TABLE_HEADER_ACTION_CLASS =
  "shrink-0 gap-1 rounded-lg px-3 py-1.5 text-xs font-bold whitespace-nowrap md:gap-1.5 md:rounded-[10px] md:px-3.5 md:py-2 md:text-sm";

/** Same footprint for outline / tertiary table-header actions. */
export const TABLE_HEADER_SECONDARY_ACTION_CLASS =
  "shrink-0 gap-1 rounded-lg px-3 py-1.5 text-xs font-medium whitespace-nowrap md:gap-1.5 md:rounded-[10px] md:px-3.5 md:py-2 md:text-sm";

export const TABLE_HEADER_ACTION_ICON_CLASS = "size-3.5 shrink-0";
